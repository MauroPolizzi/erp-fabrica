// Configuración de producción (default). En desarrollo se reemplaza por
// environment.development.ts vía fileReplacements (angular.json).
export const environment = {
  production: true,
  apiUrl: '/api',
};
